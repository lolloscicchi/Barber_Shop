import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#363062',
    width: '100%',
    alignItems: 'center',
    padding: 10,
    borderRadius: 8,
  },
  titleStyle: {
    color: 'white',
    fontSize: 16,
  },
});
